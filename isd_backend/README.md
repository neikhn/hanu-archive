### Tích hợp backend vào project qua react query 
In your client-side project folder
requirements:
    npm install axios dotenv

Sse CORS protocol - cross-origin resource sharing: 
    in your ./src/index.js